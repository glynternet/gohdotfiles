SHELL = sh

PROFILE_FILENAME ?= .vimrc
PROFILE ?= $(HOME)/$(PROFILE_FILENAME)

GOH_VIMDOTFILES_DIR ?= $(CURDIR)

install:
	ln -sf $(GOH_VIMDOTFILES_DIR)/$(PROFILE_FILENAME) $(HOME)/$(PROFILE_FILENAME)
