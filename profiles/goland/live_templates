## LiveTemplates

testt

func Test$NAME$(t *testing.T) {
	for _, tc := range []struct {
		name string
	}{
		{
			name: "zero-values",
			$END$
		},
	} {
		t.Run(tc.name, func(t *testing.T) {

		})
	}
}