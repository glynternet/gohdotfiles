SHELL = sh

PROFILE_FILENAME ?= .vimrc
PROFILE ?= $(HOME)/$(PROFILE_FILENAME)

GOH_DOTFILES ?= $(HOME)/gohdotfiles
GOH_VIMDOTFILES_DIR ?= $(GOH_DOTFILES)/vim

install:
	ln -s $(GOH_VIMDOTFILES_DIR)/$(PROFILE_FILENAME) $(HOME)/$(PROFILE_FILENAME)